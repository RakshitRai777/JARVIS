from ai.execution.execution_result import ExecutionResult


class SystemExecutor:

    def execute(self, context):

        return ExecutionResult(
            success=False,
            message="System commands are not implemented yet."
        )